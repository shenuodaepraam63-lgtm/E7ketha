import { BarChart3, Bell, BookOpen, CheckCircle2, Edit3, FileClock, LayoutDashboard, Menu, Megaphone, Save, Send, ShieldCheck, Trash2, Users, UserCog, X, Tags, Loader2, Upload, WandSparkles } from 'lucide-react';
import { useState } from 'react';
import { Link, useLocation } from 'wouter';
import { toast } from 'sonner';
import { useAuth } from '@/_core/hooks/useAuth';
import { trpc } from '@/lib/trpc';
import { AdminOperations } from './AdminOperations';
import AdminReports from './AdminReports';
import AdminQuotesManager from './AdminQuotesManager';

const nav = [{ key: 'overview', label: 'نظرة عامة', icon: LayoutDashboard }, { key: 'novels', label: 'الروايات', icon: BookOpen }, { key: 'quotes', label: 'الاقتباسات', icon: WandSparkles }, { key: 'authors', label: 'المؤلفون', icon: Users }, { key: 'genres', label: 'التصنيفات', icon: Tags }, { key: 'users', label: 'المستخدمون والأدوار', icon: UserCog }, { key: 'reports', label: 'التقارير والإحصائيات', icon: BarChart3 }, { key: 'audit', label: 'سجل النشاط', icon: FileClock }, { key: 'trash', label: 'سلة المهملات', icon: Trash2 }, { key: 'notifications', label: 'الإشعارات', icon: Bell }, { key: 'messages', label: 'الرسائل', icon: Send }, { key: 'ads', label: 'إدارة الإعلانات', icon: Megaphone }] as const;
type Section = typeof nav[number]['key'];

function AdminSidebar({ section, setSection, open, onClose }: { section: Section; setSection: (value: Section) => void; open: boolean; onClose: () => void }) { const [, navigate] = useLocation(); return <aside className={`${open ? 'translate-x-0' : 'translate-x-full'} fixed inset-y-0 right-0 z-50 flex w-72 flex-col border-l border-[#222b4a] bg-[#0d142d] text-white transition-transform md:static md:translate-x-0`}><div className="flex h-[78px] items-center justify-between border-b border-white/10 px-6"><Link href="/admin" className="text-lg font-extrabold">رِواية <span className="text-[#8d84f9]">/ admin</span></Link><button onClick={onClose} className="md:hidden"><X size={18} /></button></div><nav className="flex-1 p-4">{nav.map((item) => { const Icon = item.icon; return <button key={item.key} onClick={() => { setSection(item.key); onClose(); if (item.key === 'novels') navigate('/admin/novels'); }} className={`mb-1 flex w-full items-center gap-3 rounded-xl px-3 py-3 text-right text-xs font-semibold transition ${section === item.key ? 'bg-[#6259dd] text-white' : 'text-white/55 hover:bg-white/5 hover:text-white'}`}><Icon size={16} />{item.label}</button>; })}</nav><div className="m-4 rounded-[16px] border border-white/10 bg-white/5 p-4 text-[10px] text-white/55">كل التغييرات تُحفظ مباشرة في Supabase.</div></aside>; }

function Field({ label, value, onChange, type = 'text', required = false, placeholder }: { label: string; value: string | number; onChange: (value: string) => void; type?: string; required?: boolean; placeholder?: string }) { return <label className="grid gap-2 text-xs font-bold"><span>{label}</span><input required={required} type={type} value={value} placeholder={placeholder} onChange={(event) => onChange(event.target.value)} className="h-11 rounded-xl border border-border bg-background px-3 text-sm outline-none transition focus:border-[#8279ee]" /></label>; }

function Overview({ onSelect }: { onSelect: (section: Section) => void }) { const summary = trpc.admin.summary.useQuery(); const cards = summary.data ? [{ label: 'الروايات', value: summary.data.novels, section: 'novels' as const }, { label: 'المؤلفون', value: summary.data.authors, section: 'authors' as const }, { label: 'التصنيفات', value: '—', section: 'genres' as const }, { label: 'المستخدمون', value: 'إدارة', section: 'users' as const }] : []; return <div><div className="mb-8"><h1 className="text-2xl font-extrabold">لوحة التحكم</h1><p className="mt-2 text-xs text-muted-foreground">أدر محتوى المنصة من مكان واحد.</p></div>{summary.isLoading ? <Loader2 className="animate-spin" /> : <div className="grid gap-4 sm:grid-cols-3">{cards.map((card) => <button key={card.label} onClick={() => onSelect(card.section)} className="rounded-[20px] border border-border bg-card p-6 text-right transition hover:-translate-y-0.5 hover:border-[#8279ee]"><span className="text-xs text-muted-foreground">{card.label}</span><strong className="mt-3 block text-3xl font-extrabold">{card.value}</strong><span className="mt-2 block text-[10px] font-bold text-[#675de8]">إدارة المحتوى ←</span></button>)}</div>}</div>; }

function AuthorsManager() { const utils = trpc.useUtils(); const query = trpc.admin.authors.list.useQuery(); const create = trpc.admin.authors.create.useMutation({ onSuccess: () => { toast.success('تمت إضافة المؤلف'); reset(); void utils.admin.authors.list.invalidate(); }, onError: (error) => toast.error(error.message) }); const update = trpc.admin.authors.update.useMutation({ onSuccess: () => { toast.success('تم تحديث المؤلف'); reset(); void utils.admin.authors.list.invalidate(); }, onError: (error) => toast.error(error.message) }); const remove = trpc.admin.authors.delete.useMutation({ onSuccess: () => { toast.success('تم حذف المؤلف'); void utils.admin.authors.list.invalidate(); }, onError: (error) => toast.error(error.message) }); const [editing, setEditing] = useState<number | null>(null); const [form, setForm] = useState({ slug: '', name: '', bio: '', avatarUrl: '', bookCount: '0' }); const reset = () => { setEditing(null); setForm({ slug: '', name: '', bio: '', avatarUrl: '', bookCount: '0' }); }; const submit = (event: React.FormEvent) => { event.preventDefault(); const data = { slug: form.slug, name: form.name, bio: form.bio || undefined, avatarUrl: form.avatarUrl || undefined, bookCount: Number(form.bookCount) || 0 }; editing ? update.mutate({ id: editing, data }) : create.mutate(data); }; return <ManagerShell title="المؤلفون" description="أضف وعدّل بيانات المؤلفين المرتبطة بالروايات."><form onSubmit={submit} className="mb-8 grid gap-3 rounded-[20px] border border-border bg-card p-5 md:grid-cols-2"><Field label="الاسم" value={form.name} required onChange={(value) => setForm({ ...form, name: value })} /><Field label="الرابط المختصر" value={form.slug} required placeholder="مثال: sifr-al-bidaya — بدون https://" onChange={(value) => setForm({ ...form, slug: value })} /><Field label="الصورة URL" value={form.avatarUrl} onChange={(value) => setForm({ ...form, avatarUrl: value })} /><Field label="عدد الأعمال" type="number" value={form.bookCount} onChange={(value) => setForm({ ...form, bookCount: value })} /><label className="grid gap-2 text-xs font-bold md:col-span-2"><span>نبذة</span><textarea value={form.bio} onChange={(event) => setForm({ ...form, bio: event.target.value })} className="min-h-24 rounded-xl border border-border bg-background p-3 text-sm outline-none focus:border-[#8279ee]" /></label><div className="flex gap-2 md:col-span-2"><button className="inline-flex items-center gap-2 rounded-xl bg-[#171e42] px-4 py-3 text-xs font-bold text-white"><Save size={15} />{editing ? 'حفظ التعديلات' : 'إضافة المؤلف'}</button>{editing && <button type="button" onClick={reset} className="rounded-xl border border-border px-4 py-3 text-xs font-bold">إلغاء</button>}</div></form><div className="grid gap-3">{(query.data ?? []).map((author) => <div key={author.id} className="flex items-center gap-3 rounded-[18px] border border-border bg-card p-4"><img src={author.avatarUrl ?? ''} className="size-12 rounded-xl object-cover" alt="" /><div className="min-w-0 flex-1"><strong className="block text-sm">{author.name}</strong><span className="text-[10px] text-muted-foreground">/{author.slug} · {author.bookCount} عمل</span></div><button onClick={() => { setEditing(author.id); setForm({ slug: author.slug, name: author.name, bio: author.bio ?? '', avatarUrl: author.avatarUrl ?? '', bookCount: String(author.bookCount) }); }} className="rounded-lg p-2 text-[#675de8] hover:bg-muted" aria-label="تعديل"><Edit3 size={16} /></button><button onClick={() => { if (window.confirm('حذف المؤلف؟ لا يمكن حذف مؤلف مرتبط بروايات.')) remove.mutate({ id: author.id }); }} className="rounded-lg p-2 text-red-500 hover:bg-red-50" aria-label="حذف"><Trash2 size={16} /></button></div>)}</div></ManagerShell>; }

function GenresManager() { const utils = trpc.useUtils(); const query = trpc.admin.genres.list.useQuery(); const create = trpc.admin.genres.create.useMutation({ onSuccess: () => { toast.success('تمت إضافة التصنيف'); reset(); void utils.admin.genres.list.invalidate(); }, onError: (error) => toast.error(error.message) }); const update = trpc.admin.genres.update.useMutation({ onSuccess: () => { toast.success('تم تحديث التصنيف'); reset(); void utils.admin.genres.list.invalidate(); }, onError: (error) => toast.error(error.message) }); const remove = trpc.admin.genres.delete.useMutation({ onSuccess: () => { toast.success('تم حذف التصنيف'); void utils.admin.genres.list.invalidate(); }, onError: (error) => toast.error(error.message) }); const [editing, setEditing] = useState<number | null>(null); const [form, setForm] = useState({ slug: '', name: '', description: '', icon: '✦' }); const reset = () => { setEditing(null); setForm({ slug: '', name: '', description: '', icon: '✦' }); }; const submit = (event: React.FormEvent) => { event.preventDefault(); const data = { slug: form.slug, name: form.name, description: form.description || undefined, icon: form.icon || '✦' }; editing ? update.mutate({ id: editing, data }) : create.mutate(data); }; return <ManagerShell title="التصنيفات" description="نظّم أبواب الاكتشاف والتصنيفات الظاهرة للقراء."><form onSubmit={submit} className="mb-8 grid gap-3 rounded-[20px] border border-border bg-card p-5 md:grid-cols-2"><Field label="اسم التصنيف" value={form.name} required onChange={(value) => setForm({ ...form, name: value })} /><Field label="الرابط المختصر" value={form.slug} required onChange={(value) => setForm({ ...form, slug: value })} /><Field label="الأيقونة" value={form.icon} onChange={(value) => setForm({ ...form, icon: value })} /><label className="grid gap-2 text-xs font-bold md:col-span-2"><span>الوصف</span><textarea value={form.description} onChange={(event) => setForm({ ...form, description: event.target.value })} className="min-h-20 rounded-xl border border-border bg-background p-3 text-sm outline-none focus:border-[#8279ee]" /></label><div className="flex gap-2 md:col-span-2"><button className="inline-flex items-center gap-2 rounded-xl bg-[#171e42] px-4 py-3 text-xs font-bold text-white"><Save size={15} />{editing ? 'حفظ التعديلات' : 'إضافة التصنيف'}</button>{editing && <button type="button" onClick={reset} className="rounded-xl border border-border px-4 py-3 text-xs font-bold">إلغاء</button>}</div></form><div className="grid gap-3 sm:grid-cols-2">{(query.data ?? []).map((genre) => <div key={genre.id} className="rounded-[18px] border border-border bg-card p-4"><div className="flex items-start gap-3"><span className="grid size-10 place-items-center rounded-xl bg-[#efeeff] text-lg text-[#675de8]">{genre.icon}</span><div className="min-w-0 flex-1"><strong className="block text-sm">{genre.name}</strong><span className="text-[10px] text-muted-foreground">{Number(genre.novelCount)} رواية · /{genre.slug}</span></div><button onClick={() => { setEditing(genre.id); setForm({ slug: genre.slug, name: genre.name, description: genre.description ?? '', icon: genre.icon ?? '✦' }); }} className="text-[#675de8]" aria-label="تعديل"><Edit3 size={16} /></button><button onClick={() => { if (window.confirm('حذف التصنيف؟ ستتم إزالة ارتباطه بالروايات.')) remove.mutate({ id: genre.id }); }} className="text-red-500" aria-label="حذف"><Trash2 size={16} /></button></div><p className="mt-3 text-xs leading-6 text-muted-foreground">{genre.description}</p></div>)}</div></ManagerShell>; }
